sudo -i -u postgres       # Login as the Postgres user
sudo -u postgres psql     # Accessing a Postgres Prompt as the current user 
postgres -V               # View Server version
psql -V                   # View client version  
\l                        # List databases
\q                        # exit

\du                       # list all accounts in postgres
\du+


CREATE DATABASE guru99;   # create db
DROP DATABASE guru99;

\c guru99                 # Connect to a Database 
\dt                       # List tables 
SELECT * FROM  table      # List objects in a table
\d                        # Describe table
\d+                       # Describe table with more details 
